# Tarea supervisada: OpenCode + DeepSeek en iparts-ecommerce

Repo: /Users/oviedo/Documents/GitHub/iparts-ecommerce
Dev server: http://localhost:3000 (ya corre). ERP API prod: https://api.ipartsve.com
ERP monorepo hermano: /Users/oviedo/Documents/GitHub/IPARTS-platfotrm (NO reescribir el ERP salvo el módulo ecommerce si hace falta el contrato).

## Contexto ya hecho (NO rehacer desde cero)
- Shop Next.js 15, General Sans, azules IPARTS (#0284c7 / #0369a1 / #0c4a6e).
- Árbol de catálogo en `src/components/catalog-tree.tsx` + títulos de posición en `src/lib/position-title.ts`.
- Página `/c/[category]` con categoría → modelo → calidad → marca.
- Fotos con watermark IPARTS + sello de marca (`product-photo.tsx`).
- Pagos: `src/lib/payment-split.ts` (2.5% wallet dueño), `payment-methods.ts`, `docs/payments.md`.
- Checkout parte bruto / procesador / dueño / neto. `/owner` es el ledger.
- Binance Pay = plugin (0% on-network publicado). Zelle = manual, no API merchant. Stripe no en VE.
- Stock: `loadShopCatalog` llama GET /ecommerce/catalog. Prod responde 401 porque falta ECOMMERCE_API_KEY en Dokploy. Fallback preview.

## Qué debes hacer (prioridad)
1. Mejorar la estética del árbol/listado para que no se vea genérico: más mostrador de partes (MobileSentrix/Repasaas), sin chips sueltos, títulos de posición claros. Verificar en localhost:3000/c/pantallas.
2. Si queda tiempo: pulir ficha de producto y checkout (desglose de pasarela visible).
3. Tests unitarios de lo que toques (`vitest run`). No inventar e2e masivo.

## Prohibido
- Inventar pasarela o cobrar un % de Binance/Zelle que no esté en docs oficiales.
- Guardar passwords en localStorage.
- Overwrite Device.salePrice en el ERP.
- Mass-fix lints. No tocar ~308 lints.
- Inventar políticas de garantía/comisión de ventas.
- Dejar el shop bajo src/app del ERP.

## Al terminar
Envía worker_done desde TU terminal con outcome succeeded/failed y resumen de 3 oraciones + files-modified.
